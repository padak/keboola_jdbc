{
  "ui:order": [
    "name",
    "keboolaStack",
    "customConnectionUrl",
    "token",
    "projectId",
    "branchId",
    "workspaceId"
  ],
  "token": {
    "ui:widget": "password",
    "ui:help": "Either a Storage API token (project Settings > API Tokens) or a Personal Access Token starting with kbc_pat_ (user Account Settings). The kind is recognised from the token itself."
  },
  "keboolaStack": {
    "ui:help": "Select your Keboola stack. Choose 'Custom URL' for single-tenant or private deployments."
  },
  "customConnectionUrl": {
    "ui:help": "Only needed when 'Custom URL' is selected. e.g., connection.yourcompany.keboola.com"
  },
  "projectId": {
    "ui:help": "Needed only with a Personal Access Token, which is not tied to one project. Leave empty — you will be prompted to select a project when connecting. Ignored for a Storage API token."
  },
  "branchId": {
    "ui:help": "Leave empty — you will be prompted to select a branch when connecting. Or enter a specific Branch ID manually."
  },
  "workspaceId": {
    "ui:help": "Leave empty — you will be prompted to select a workspace when connecting. Or enter a specific Workspace ID manually."
  }
}
